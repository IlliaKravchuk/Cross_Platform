@model IEnumerable<Lab6.Models.BehaviourMonitoring>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MonitoringDetails)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MonitoringDetails)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.StudentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BehaviourMonitoringId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BehaviourMonitoringId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BehaviourMonitoringId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
