@model IEnumerable<Lab6.Models.StudentLoan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateOfLoan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AmountOfLoan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LoanDetails)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfLoan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AmountOfLoan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoanDetails)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.StudentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StudentLoanId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StudentLoanId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StudentLoanId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
