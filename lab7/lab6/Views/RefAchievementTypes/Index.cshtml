@model IEnumerable<Lab6.Models.RefAchievementType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AchievementTypeDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AchievementTypeDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AchievementTypeCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AchievementTypeCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AchievementTypeCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
