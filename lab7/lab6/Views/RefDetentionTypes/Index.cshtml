@model IEnumerable<Lab6.Models.RefDetentionType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DetentionTypeDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DetentionTypeDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DetentionTypeCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DetentionTypeCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DetentionTypeCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
